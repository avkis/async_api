export PROJECT_ROOT=$(pwd)
export APISERVER_HOST=localhost
export APISERVER_PORT=5000
export DB_PORT=5432
export DB_PORT_TEST=5433
export DB_USER=admin
export DB_PASSWORD=secret
export DB_NAME=asyncapi
export DB_SSL_MODE=disable
export DB_SCHEMA=init_schema
export DB_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSL_MODE}
export DB_URL_TEST=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT_TEST}/${DB_NAME}?sslmode=${DB_SSL_MODE}
export JWT_SECRET=supersecretkey
export JWT_ACCESS_TOKEN_LIFETIME=15
export JWT_REFRESH_TOKEN_LIFETIME=5d

export LOCALSTACK_DOCKER_NAME=localstack_container
export LOCALSTACK_VOLUME_DIR=~/localstack

export S3_LOCALSTACK_ENDPOINT=http://s3.localhost.localstack.cloud:4566
export SQS_LOCALSTACK_ENDPOINT=http://localhost:4566

export AWS_ACCESS_KEY_ID=dummy
export AWS_SECRET_ACCESS_KEY=dummy
export AWS_DEFAULT_REGION=eu-west-1
export SQS_QUEUE=report-sqs-queue
export S3_BUCKET=api-reports

# TerraForm's variables
export TF_VAR_aws_access_key_id=${AWS_ACCESS_KEY_ID}
export TF_VAR_aws_secret_access_key=${AWS_SECRET_ACCESS_KEY}
export TF_VAR_aws_default_region=${AWS_DEFAULT_REGION}
export TF_VAR_sqs_queue=${SQS_QUEUE}
export TF_VAR_s3_bucket=${S3_BUCKET}
export TF_VAR_s3_localstack_endpoint=${S3_LOCALSTACK_ENDPOINT}
export TF_VAR_sqs_localstack_endpoint=${SQS_LOCALSTACK_ENDPOINT}